#include "queue_algorithms.h"

void round_robin(int ** queues, int queue_length, int n_queues)
{
    int next_served = 0;
    int i, j;

    for (i = 0; i < queue_length; i++)
    {
        for (j = 0; j < n_queues; j++)
        {
            if (queues[i][j] == 1)
        }
    }
}
